<form method="post" action="{{ path('subscription_delete', {'user_id': subscription.user_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscription.user_id) }}">
    <button class="btn">Delete</button>
</form>