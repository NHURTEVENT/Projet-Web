{% extends 'base.html.twig' %}

{% block title %}Basket{% endblock %}

{% block body %}
    <h1>Basket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ basket.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('basket_index') }}">back to list</a>

    <a href="{{ path('basket_edit', {'user_id': basket.user_id}) }}">edit</a>

    {{ include('basket/_delete_form.html.twig') }}
{% endblock %}