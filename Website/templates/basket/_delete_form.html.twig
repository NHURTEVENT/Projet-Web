<form method="post" action="{{ path('basket_delete', {'user_id': basket.user_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ basket.user_id) }}">
    <button class="btn">Delete</button>
</form>